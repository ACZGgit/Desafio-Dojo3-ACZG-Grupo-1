class app {
    static int controleDasAbas(String entrada){

        //String entrada = "3,5,F,F,C,C,C"
        //int saida = 2

        int inicial_abas, acoes_pericles;

        for(int i=0;i< entrada.length();i++)
        {
            if(entrada[i]!=',')
            {

            } else if {
                entrada
            }
            break

            if {
            inicial_abas
        }
        }
        }

    }
}
